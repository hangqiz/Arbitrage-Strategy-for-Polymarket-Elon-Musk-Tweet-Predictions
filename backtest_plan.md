# 基于 tweets.db 的回测方案

目标：在低频率（周级）数据上复现生产策略「四硬门控 + 概率模型 + 三腿组合」，对历史事件回放，统计触发率、胜率、PnL、回撤，并给出可校准的灵敏度结论。本文档对应当前 `tweets.db` 真实数据。

---

## 1. 数据盘点（tweets.db）

| 表 | 行数 | 说明 |
|---|---|---|
| `tweets` | 11,517 | 发帖时间序列，`created_at` 区间 2025-10-31 → 2026-08-19 |
| `events` | 131 | 周级发帖市场（`slug LIKE 'elon-musk-of-tweets%'`） |
| `history_pricing` | 5,066,600 | 每事件×区间×时间戳 的 pricer 概率；7 种 pricer |
| `history_implied` | 175,350 | pricer 推算的**隐含发帖数**（非价格），辅助信号用 |
| `token_prices` | 3,883,230 | 该数据集中**这些周事件无对应 token 报价**（实测 0 行） |

关键结论：

- **唯一可用的“区间价格信号”是 `history_pricing.prob`**（以 概率×100 充当 Yes 中间价）。
  它并非真实盘口，而是 pricer（MonteCarlo/WeekdayHour 等）模型输出，无买卖价差、无深度。
- `token_prices` 对这些周事件无数据，因此本回测**无法复现真实盘口/滑点/流动性**。
- 每条事件按 `tweetCount`（官方结算数）定位胜出区间做结算。

### 可用事件筛选
- 需同时满足：① 有 `startTime`/`endDate`/`tweetCount` 元数据；② 有目标 pricer 的定价行。
- 实测（SimpleMonteCarlo）：**131 事件中仅 19 个完全可用**。
  排除原因：`无该 pricer 定价行`（56 个，多为 1、5–8 月，落在定价窗口外）
  或 `缺结算数/时间元数据`（56 个，有定价但无结算数 → 无法结算）。

> 19 个样本对低频策略而言**非常少**，任何结论都只能算“诊断”，不能作为可上线的胜率证明。

---

## 2. 回测方法

### 2.1 数据加载与修复（loader.py）
- 从 `history_pricing` 组装「事件 ×(区间×时间)」概率矩阵，时间轴 = 所有 `distinct timestamp` 的并集。
- **已修复的严重 bug**：原实现用 `len(brackets)-1` 作为区间索引，一旦所有区间注册完毕（首个时间戳 30 个后），后续每个 ts 的每条记录都覆盖到最后一个区间，导致 1308/1309 个时间戳概率全为 0。改为 `bracket_idx[key]` 按 (lo,hi) 稳定索引。
- 新增 `live` 掩码：该时间戳原始概率质量 > 1e-9 才视为“市场活跃”，全 0 时刻不更新爆发状态机（避免把“未开盘/模型未产出”的 0 价误锚定为安静起始价）。

### 2.2 事件回放（engine.py）
对每条可用事件，按时间戳顺序逐步执行生产逻辑：
1. **日历门**：`days_elapsed >= night_day` 且 `hours_to_end > settle_buffer_h`。
2. **发帖速率/爆发/安静**：`evaluate_burst` + `BurstTracker` 状态机；`record_quiet_start` 锚定安静起始价。
3. **过热度 x** = 热门区间（Yes 价最高）当前价 − 安静起始价；`x > 0`。
4. **回落概率** `p_success(x, H, params)` ≥ `p_min`。
5. 全部通过 → 三腿组合（腿1 热门 No＝做空过热；腿2 受害者 Yes＝做多被错压；腿3 上档≤15¢ Yes＝尾部对冲），按 `equity × risk_pct` 分配预算。
6. **结算**：按 `tweetCount` 落点判胜出区间，逐腿结算 `shares×(payout−cost) − fee`；每事件只取首个达标信号入场。

### 2.3 成本假设
- `trade_cost_c = 1.0¢/份`（flat），按份数×单价成交，无滑点（价=中间价=买卖价）。

---

## 3. 基线结果（生产默认参数）

覆盖：**19/131 可回测** → 触发 **7 笔**交易。

```
n_trades   : 7
win_rate   : 14.3%  (1/7)
pnl_usd    : -198.72
avg_pnl    : -28.39
max_drawdown: -198.72
total_fee  : 398.95
avg_x      : 16.36¢
avg_p      : 0.609   ← 模型平均概率 ~61%
avg_hours_to_end: 11.05h
```

跳过原因（样本点级）：`calendar` 10,329 · `quiet` 7,104 · `p_min` 3,769 · `x<=0` 1,210。

### 3.1 核心诊断
1. **模型概率明显偏乐观**：入场样本 `avg_p≈0.61`，实际胜率仅 14%。`p_success` 的 `μ_quiet/k_shock/τ/σ_s/δ` 用 pricer 概率路径偏差较大 → 需重新校准。
2. **费用拖累严重**：7 笔总费用 $399 接近并超过毛损。腿2/腿3 取低单价 token（数 ¢），份数被放大，flat 1¢/份 费用占比高。需单价下限、成交滑点建模。
3. **入场普遍在临近结算（~11h）**，符合“末段情绪”设定，但留给回落时间短，叠加费用，胜率被压到 1/7。

---

## 4. 灵敏度 / 校准（sweep）

仅探最关键的两个旋钮（p_min × delta = 所需回落 ¢），不遍历式强拟合：

| p_min | delta | trades | win_rate | pnl | avg_pnl |
|---|---|---|---|---|---|
| 0.60 | 4 | 7 | 14% | **−198.72** | −28.4 |
| 0.60 | 6 | 4 | 0% | −154.40 | −38.6 |
| 0.60 | 8 | 4 | 25% | +9.80 | +2.5 |
| 0.65 | 4 | 4 | 25% | +77.18 | +19.3 |
| 0.65 | 6 | 2 | 50% | +106.70 | +53.4 |
| 0.65 | 8 | 1 | 100% | +21.06 | +21.1 |
| ≥0.70 | any | 0 | – | 0 | – |

**解读**：提高回落要求（δ↑）与概率门槛（p_min↑）会把胜率与期望均值明显改善，方向正确。
**但样本 n≤7，属诊断而非实证**——把 δ=6/p_min=0.65 之类直接当生产参数是过拟合风险。

---

## 4.1 盘中 1 分钟回测（低频率→盘中捕捉）

目标改为盘中逐分钟捕捉价格波动套利，重采样与轮询同步到 **1 分钟**。

### 数据与频率说明
- 原生 `history_pricing` 价格**约 10 分钟一档**（600s 为主），**无原生 1 分钟价格**。
- 处理：`resample(ev, dt=60)` 把每条事件价格**前向填充为 1 分钟网格**（每步都有完整 30 档价格矢量）；开盘前 ~8h 缺口用首个观测分布回填，维持零和（归一化到 1）。→ 解决“每个时间窗口都必须同时有数据与价格”。
- 轮询：`main.py` 的 `POLL_INTERVAL_SEC` 默认 300→**60**；回测用 `--poll-sec 60`。

### 运行
```bash
# 限定两条完整周，1 分钟回测
python scripts/run_backtest.py --poll-sec 60 --slugs "february-20-february-27,february-24-march-3"

# 全部 19 事件，1 分钟
python scripts/run_backtest.py --poll-sec 60
```

### 结果
- **限定两周（feb20/feb24）**：**0 笔入场**——两个二月周里四硬门控从未同时满足（无过热度/无安静确认后的超阈值回落）。
- **全 19 事件、1 分钟**：仍 **7 笔**、**胜率 0/7**、PnL **−$138.3**、费用 **$321**、`avg_p≈0.605`。
  - 与 10 分钟基线（1 胜、−$199）对比：入场时点因分钟级栅格微移，**结果对轮询/入场节奏敏感**——这是健壮性黄灯。

### 诊断
1. 样本选择效应大：选“安静的二月周”无任何信号，选“活跃的三四月周”才有入场 → 两点取样不能代表整体。
2. 1 分钟粒度不变更模型口径：`avg_p≈0.6` 依旧高估（0 胜），费用拖累依旧。
3. 前向填充只提供“每分钟再看一眼”的评估节奏，**不会凭空产生盘中新信息**；真实的分钟级尖峰/跳空仍无法从该数据复现（需真实 token 盘口）。

### 参数调整：仅保留腿1+腿2，精力集中在腿1（热门 No）
原高费用主因是腿2/腿3 买到 ≈0.6¢ 的 token，天量份数 × 1¢/份费用。调整（config `LegParams`）：
- 价格下限：新增 `leg2_floor_c=8.0¢`、把 `leg3_floor_c` 从 1.0→4.0¢ 并**真正执行**（原未生效）。
- 预算：`leg2_ratio 0.60→0.30`、`leg3_ratio 0.12→0.08`，腿1（预算 $200）成为绝对核心。
- **腿3 对冲默认关闭**（`hedge_enabled=False`）：低价“赌博区间”仓位对冲意义不大且纯烧费用；仅保留腿1（做空热门）+ 腿2（做多受害者）。留开关等拿到优质数据再评估。

**1 分钟全 19 事件重跑**：费用 **$321→$49**（−85%），腿2/腿3 份数从数千降至数百、腿3 不出单；胜率 0/7→**7/7**、PnL −$138→**+$1070**。
**关键洞察**：所有 7 笔入场 `hot_won=False`——即“热门被高估、最终未胜出”，**腿1 买热门 No 本就是真正的盈利源**（腿2 提供少量加成，腿3 空耗费用）。
⚠️ 但 **7 笔全胜是小样本幸运**（一旦热门真赢，腿1 亏 $200），且费用仍按 flat 1¢/份。需在补齐 `tweetCount` 扩大样本后再验证健壮性。

### 滚动出场（buy NO → NO 上涨提前止盈，规避结算尾部风险）
用户要求不在过热买入 NO 后一直持有到结算，而是情绪降温、NO 上升时提前卖出，多次锁利、规避「买入区间最终获胜」的巨额尾部风险。已实现 `RoundTripParams`（engine `_run_roundtrip`）：
- 入场不变（四门+概率，买热门 NO）；持仓中按**本桶 NO 现值**计盈亏（关键修正：用入场桶自身 Yes 价，而非全市场最大热门价）。
- 出场：**TP 止盈（NO ≥ +12¢）｜SL 止损（默认关闭）｜结算强制平仓**，可此后再次入场。
- **实测结论**：这套数据里**止损系统性有害**——反转前必经一个急杀，止损恰好打在坑底（SL=25/30 时暴杀笔近全损，把整轮拖负）；改为**只止盈不止损**（TP-only）后胜率 7/7、**+$511**，其中 6 笔由 TP 提前锁利、仅 1 笔持有到结算。
- **权衡**：持有到结算在本次样本更赚（+$1070）但全额暴露结算尾部；TP-only 少赚些（+$511）但**把结算尾部敞口大幅压缩**，符合风控优先的意图。若日后样本真出现「热门获胜」，持有版会吃 $200/笔尾部，TP-only 在崩溃前多为 TP 出场、回避该尾部。

---

## 5. 评估口径与验收门

| 指标 | 说明 |
|---|---|
| 触发率 | 入场事件 / 可用事件，过低说明门控过紧（本数据 7/19） |
| 胜率 / 期望 | win_rate、avg_pnl_usd、（PnL − fee） |
| 回撤 | 按时间序累计 PnL 计算 max_drawdown |
| 过拟合控制 | 灵敏度扫描只做复核，不据此逐点挑最优；候选参数须在**留出子区间**验证 |

**口径明确**：本回测是“信号/门控在 pricer 概率路径上的 cold replay”，**不代表真实可交易收益**。真正验收需真实盘口价格历史。

---

## 6. 已知局限与后续

1. **样本太小**：仅 19 可用事件 / 7 笔。需扩充：
   - 拉取更长时间覆盖的 `history_pricing` 与真实 `token_prices`（当前周事件无 token 报价）。
   - 对“有定价但缺结算数”的 56 个事件补 `tweetCount`。
2. **无真实盘口**：价=中间价、无价差/滑点/深度 → 回调 `max_spread_c` 门控无法离线验证。
3. **费用模型粗糙**：flat 1¢/份 对低价 leg 不利；建议按 CLOB 实际 taker 费 + 最小计价单位建模。
4. **概率模型未校准**：p_success 在 pricer 路径上系统性偏乐观，是首要整改项（calibration 模块）。
5. **催化剂/事件重叠门**未在离线引擎内落地（无法离线推未来事件）→ 生产中仍保留。

---

## 7. 运行方式

```bash
# 完整回测 + 覆盖统计
python scripts/run_backtest.py --out results/backtest.json

# 关键参数灵敏度扫描（p_min × delta）
python scripts/run_backtest.py --sweep --out results/backtest.json

# 换 pricer（MonteCarlo1Month / WeekdayHour 等）
python scripts/run_backtest.py --pricer MonteCarlo1Month --out results/backtest_mcm.json
```

输出：控制台汇总 + `results/backtest.json`（覆盖全表、逐事件、逐笔明细、灵敏度网格）。